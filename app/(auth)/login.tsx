import { View, Text } from "react-native";

const LoginScreen = () => {
  return (
    <View className="bg-background flex-1">
      <Text>login</Text>
    </View>
  );
};

export default LoginScreen;
